{
  "name": "chrome-tab-manager",
  "version": "1.0.0",
  "description": "Advanced Chrome Tab Manager Extension with TypeScript",
  "scripts": {
    "build": "esbuild src/background/service-worker.ts src/popup/popup.ts src/content/content-script.ts src/sidepanel/sidepanel.ts --bundle --outdir=dist --target=chrome114 --format=esm --splitting",
    "watch": "npm run build -- --watch",
    "clean": "rm -rf dist extension",
    "package": "npm run build && rm -rf extension && mkdir -p extension && cp manifest.json popup.html sidepanel.html styles.css extension/ && cp -r icons dist extension/ && echo 'Extension packaged to ./extension folder (ready to load in Chrome)'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {}
}
